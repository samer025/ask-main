<form method="post" action="{{ path('app_fiche_technique_delete', {'id': fiche_technique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_technique.id) }}">
    <button class="btn">Delete</button>
</form>
